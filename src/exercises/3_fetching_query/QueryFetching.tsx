import { useState } from "react"
import { useGetOneQuery } from "./queryFetchingApi"
import DisplayObject from "../../components/DisplayObject"

/**
 * TODO: Use your generated hook and display the resolved data using the Display Object!
 * You will also need a simple input field to provide the object id!
 */
export default function QueryFetching() {

    return (
        <>
            <h1>QueryFetching</h1>

            {/* add an input field for the object Id */}

            {/* use the imported Display Object to display the data!  */}
        </>
    )
}
