import { useGetAllQuery } from "./selectingApi"

/**
 * TODO: Instead of displaying all the data, try just showing the name!
 */
export default function Selecting() {

    // Make this work with your request somehow. `names` should be a `string[]`
    // const { names } = useGetAllQuery(undefined)

    return (
        <>
            <h1>Selecting</h1>

            {/* Display the names somehow  */}
        </>
    )
}
