import { useGetAllQuery } from "./loadingStatesApi"

/**
 * TODO: display whenever your request is currently trying to retrieve data from the API
 * It helps to add a button that `refetches` the requested data. 
 */
export default function LoadingStates() {

    return (
        <>
            <h1>LoadingStates</h1>

            {/* add a button to refetch the request */}

            {/* display if the data is currently being retrieved or not */}
        </>
    )
}
