import { useState } from "react"
import { useGetOneQuery } from "./errorHandlingApi"
import DisplayObject from "../../components/DisplayObject"

/**
 * TODO: You can copy & paste your solution for 3_fetching_query here.
 * Now add some error handling for whenever a request returns a 404 or similar! 
 */
export default function ErrorHandling() {

    return (
        <>
            <h1>Error Handling</h1>

            {/* add an input field for the object Id */}

            {/* show the error whenever an error occures */}

            {/* use the imported Display Object to display the data whenever there is no error!  */}
        </>
    )
}
