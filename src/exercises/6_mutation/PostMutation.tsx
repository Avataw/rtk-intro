import { useState } from "react";
import { useCreateMutation } from "./postMutationApi";

/**
 * TODO: We need a way to input a name and use our POST request!
 */
export default function PostMutation() {

    return (
        <>
            <h1>Post Mutation</h1>

            {/* add an input field for the name */}

            {/* add a button that uses the name to create an object */}

            {/* display the response from the POST request */}
        </>
    )
}
